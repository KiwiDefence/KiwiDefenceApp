# Authentication
AUTH_EMAIL=admin@example.com
AUTH_PASSWORD=your-secure-password
AUTH_NAME=Admin User

# JWT signing secret (generate with: openssl rand -base64 32)
AUTH_SECRET=generate-a-real-secret-here-min-32-chars-long!!
